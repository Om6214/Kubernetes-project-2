apiVersion: v1
kind: Service
metadata:
  name: booking-svc
  namespace: event
spec:
  selector:
    app: booking-app
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3003
      targetPort: 3003
