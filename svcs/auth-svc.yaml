apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: event
spec:
  selector:
    app: auth-app
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
