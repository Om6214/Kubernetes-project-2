apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: event
spec:
  selector:
    app: frontend-app
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
