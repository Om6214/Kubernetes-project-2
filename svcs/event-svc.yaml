apiVersion: v1
kind: Service
metadata:
  name: event-svc
  namespace: event
spec:
  selector:
    app: event-app
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3002
      targetPort: 3002
